{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "tsc src test",
            "type": "shell", //"typescript",
            //"tsconfig": "tsconfig.json",
           // "option": "watch",
           // "problemMatcher": [
           //     "$tsc-watch"
          //  ],
            "command": "tsc -b src test -w",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}