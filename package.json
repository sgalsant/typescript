{
  "name": "miproyecto",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "bin": {
    "typescript": "app.test.js"
  },
  "directories": {
    "test": "test"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/chai": "^4.2.11",
    "@types/mocha": "^7.0.2",
    "@types/node": "^13.9.5",
    "chai": "^4.2.0",
    "mocha": "^7.1.0",
    "nodemon": "^2.0.2",
    "rimraf": "^3.0.2",
    "ts-node": "^8.8.1",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "start": "node dist/app.js",
    "start:build": "npm run build && npm run start",
   
    "start:live": "nodemon",
    "start:live:build": "tsc -b src test && npm run start",

    "delete": "rimraf ./dist && rimraf ./dist-test",
    "build": "npm run delete && tsc -b src test",
    "build:live": "npm run delete && tsc -b src test -w",
    
    "test": "npm run build && ./node_modules/.bin/mocha  --reporter spec dist-test/**/*.test.js"
  },
  "author": "Santiago Galván Sánchez",
  "license": "ISC"
}
